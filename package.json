{
  "name": "warify",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "node test",
    "build": "mvn clean package && rm -rf war-template && cp -r target/warify-1 war-template"
  },
  "bin": "./index.js",
  "dependencies": {
    "archiver": "^2.0.0",
    "fs-extra": "^4.0.1"
  }
}
