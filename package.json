{
  "name": "@rogue-zero/warify",
  "version": "0.1.4",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "node test",
    "build": "mvn clean package && rm -rf war-template && cp -r target/warify-1 war-template",
    "release": "npm run build && np"
  },
  "dependencies": {
    "archiver": "^2.0.0",
    "fs-extra": "^4.0.1",
    "get-installed-path": "^3.0.1"
  },
  "devDependencies": {
    "np": "^2.16.0"
  }
}
